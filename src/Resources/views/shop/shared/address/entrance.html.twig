{% set address = hookable_metadata.context.address %}

{% if address.entrance is not null %}
    <span>{{ "monsieurbiz_coliship.ui.address.entrance"|trans({"%entrance%": address.entrance}) }}</span>
{% endif %}
